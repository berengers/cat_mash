export function cats(state=[], action){
  switch (action.type) {
    case "expression":

    default:
      return state
  }
}

export function vsCats(state=[], action){
  switch (action.type) {
    case "expression":

    default:
      return state
  }
}
